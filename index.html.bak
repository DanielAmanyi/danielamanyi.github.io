<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Amanyi Daniel — DevOps / MLOps Engineer</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400&family=Syne:wght@400;600;700;800&family=DM+Mono:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #080b10;
    --bg2: #0c1118;
    --bg3: #111820;
    --border: #1a2535;
    --border2: #243045;
    --accent: #00d4ff;
    --accent2: #0088bb;
    --accent3: #004466;
    --amber: #f5a623;
    --amber-dim: #7a5212;
    --green: #00ff88;
    --green-dim: #00442200;
    --red: #ff4444;
    --text: #c8d8e8;
    --text-dim: #5a7080;
    --text-bright: #eaf4ff;
    --mono: 'Space Mono', monospace;
    --sans: 'Syne', sans-serif;
    --code: 'DM Mono', monospace;
    --grid: rgba(0, 212, 255, 0.03);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--mono);
    font-size: 14px;
    line-height: 1.7;
    overflow-x: hidden;
  }

  /* GRID OVERLAY */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(var(--grid) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  /* SCANLINE */
  body::after {
    content: '';
    position: fixed;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.08) 2px,
      rgba(0,0,0,0.08) 4px
    );
    pointer-events: none;
    z-index: 1000;
  }

  /* NOISE TEXTURE */
  .noise {
    position: fixed;
    inset: 0;
    opacity: 0.025;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 999;
  }

  /* LAYOUT */
  .container { max-width: 1100px; margin: 0 auto; padding: 0 32px; position: relative; z-index: 2; }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    border-bottom: 1px solid var(--border);
    background: rgba(8,11,16,0.92);
    backdrop-filter: blur(12px);
  }

  .nav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 32px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    font-family: var(--sans);
    font-weight: 800;
    font-size: 15px;
    color: var(--accent);
    letter-spacing: 0.05em;
    text-decoration: none;
  }

  .nav-logo span { color: var(--text-dim); font-weight: 400; }

  .nav-links { display: flex; gap: 32px; list-style: none; }

  .nav-links a {
    color: var(--text-dim);
    text-decoration: none;
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--accent); }

  /* STATUS BAR */
  .status-bar {
    position: fixed;
    bottom: 0; left: 0; right: 0;
    z-index: 100;
    height: 28px;
    border-top: 1px solid var(--border);
    background: rgba(8,11,16,0.95);
    display: flex;
    align-items: center;
    padding: 0 24px;
    gap: 24px;
    font-size: 11px;
    color: var(--text-dim);
    font-family: var(--code);
  }

  .status-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 6px var(--green);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  .status-item { display: flex; align-items: center; gap: 8px; }
  .status-accent { color: var(--accent); }

  /* HERO */
  #hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 56px;
    position: relative;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -200px; left: -200px;
    width: 700px; height: 700px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(0,212,255,0.06) 0%, transparent 70%);
    pointer-events: none;
    will-change: transform;
  }

  .hero-glow-2 {
    position: absolute;
    bottom: -200px; right: -100px;
    width: 500px; height: 500px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(0,136,187,0.04) 0%, transparent 70%);
    pointer-events: none;
    will-change: transform;
  }

  .hero-glow-3 {
    position: absolute;
    top: 40%; left: 60%;
    width: 400px; height: 400px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(245,166,35,0.03) 0%, transparent 70%);
    pointer-events: none;
    will-change: transform;
  }

  .section-glow {
    position: absolute;
    width: 600px; height: 600px;
    border-radius: 50%;
    pointer-events: none;
    will-change: transform;
    z-index: 0;
  }

  .hero-content { padding: 80px 0; }

  .hero-top {
    display: grid;
    grid-template-columns: 70% 30%;
    gap: 40px;
    align-items: center;
    margin-bottom: 36px;
  }

  @media (max-width: 760px) {
    .hero-top { grid-template-columns: 1fr; }
    .hero-portrait-wrap { order: -1; max-width: 200px; margin: 0 auto 24px; }
  }

  .hero-portrait-wrap,
  .bracket-frame {
    position: relative;
    overflow: hidden;
  }

  .hero-portrait-wrap {
    width: 100%;
    aspect-ratio: 1;
    border: 1px solid var(--border2);
    background: var(--bg2);
  }

  .hero-portrait-wrap::before,
  .bracket-frame::before {
    content: '';
    position: absolute;
    inset: 0;
    border: 1px solid var(--accent3);
    z-index: 3;
    pointer-events: none;
  }

  .frame-corner {
    position: absolute;
    width: 16px; height: 16px;
    z-index: 4;
    pointer-events: none;
  }

  .frame-corner.tl { top: 0; left: 0; border-top: 2px solid var(--accent); border-left: 2px solid var(--accent); }
  .frame-corner.tr { top: 0; right: 0; border-top: 2px solid var(--accent); border-right: 2px solid var(--accent); }
  .frame-corner.bl { bottom: 0; left: 0; border-bottom: 2px solid var(--accent); border-left: 2px solid var(--accent); }
  .frame-corner.br { bottom: 0; right: 0; border-bottom: 2px solid var(--accent); border-right: 2px solid var(--accent); }

  /* Reusable attention-blink trigger: add .attention to any .bracket-frame
     (or .hero-portrait-wrap) to make its corner brackets blink. Add .lit
     (or .calm) to settle them into a steady glow instead. */
  .hero-portrait-wrap.attention .frame-corner,
  .bracket-frame.attention .frame-corner {
    animation: corner-blink 0.4s ease-in-out infinite;
  }

  @keyframes corner-blink {
    0%, 100% { opacity: 1; border-color: var(--accent); }
    50% { opacity: 0.25; border-color: var(--accent); }
  }

  .hero-portrait-wrap.lit .frame-corner,
  .bracket-frame.calm .frame-corner {
    animation: none;
    opacity: 1;
    border-color: var(--accent);
  }

  .hero-portrait {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: brightness(0.15) saturate(0.6) contrast(1.05);
  }

  .portrait-tag {
    position: absolute;
    bottom: 8px; left: 8px;
    font-family: var(--code);
    font-size: 9px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    background: rgba(8,11,16,0.8);
    padding: 3px 7px;
    z-index: 3;
    opacity: 0;
    transition: opacity 0.6s ease;
  }

  .hero-portrait-wrap.lit .portrait-tag { opacity: 1; }

  .hero-tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    border: 1px solid var(--border2);
    background: var(--bg2);
    padding: 6px 14px;
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 32px;
  }

  .hero-tag::before {
    content: '';
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
    animation: pulse 2s infinite;
  }

  h1.hero-name {
    font-family: var(--sans);
    font-weight: 800;
    font-size: clamp(38px, 6vw, 68px);
    line-height: 0.95;
    color: var(--text-bright);
    letter-spacing: -0.02em;
    margin-bottom: 0;
  }

  h1.hero-name .line2 {
    color: transparent;
    -webkit-text-stroke: 1px rgba(0,212,255,0.4);
  }

  /* TERMINAL */
  .hero-terminal {
    border: 1px solid var(--border2);
    background: var(--bg2);
    max-width: 640px;
    margin-bottom: 40px;
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border-bottom: 1px solid var(--border);
    background: var(--bg3);
  }

  .terminal-dot {
    width: 9px; height: 9px;
    border-radius: 50%;
    background: var(--border2);
  }

  .terminal-bar-label {
    margin-left: 8px;
    font-family: var(--code);
    font-size: 10px;
    color: var(--text-dim);
    letter-spacing: 0.05em;
  }

  .terminal-body {
    padding: 18px 20px;
    font-family: var(--code);
    font-size: 13px;
    line-height: 1.85;
    min-height: 140px;
  }

  .term-prompt-line { color: var(--text-dim); margin-bottom: 6px; }
  .term-prompt-line .term-accent { color: var(--accent); }

  .term-output {
    color: var(--text);
  }

  .term-output .tc { color: var(--text); }

  .term-cursor {
    display: inline-block;
    width: 7px; height: 13px;
    background: var(--accent);
    vertical-align: -2px;
    margin-left: 1px;
    animation: blink 1s infinite;
  }

  .hero-actions { display: flex; gap: 16px; flex-wrap: wrap; }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    font-family: var(--mono);
    font-size: 12px;
    letter-spacing: 0.08em;
    text-decoration: none;
    text-transform: uppercase;
    cursor: pointer;
    border: none;
    transition: all 0.2s;
    position: relative;
  }

  .btn-primary {
    background: var(--accent);
    color: var(--bg);
    font-weight: 700;
    clip-path: polygon(0 0, calc(100% - 8px) 0, 100% 8px, 100% 100%, 8px 100%, 0 calc(100% - 8px));
  }

  .btn-primary:hover {
    background: #33dfff;
    transform: translateY(-1px);
    box-shadow: 0 4px 20px rgba(0,212,255,0.3);
  }

  .btn-ghost {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--border2);
    clip-path: polygon(0 0, calc(100% - 8px) 0, 100% 8px, 100% 100%, 8px 100%, 0 calc(100% - 8px));
  }

  .btn-ghost:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .hero-stats {
    display: flex;
    gap: 40px;
    margin-top: 60px;
    padding-top: 32px;
    border-top: 1px solid var(--border);
  }

  .stat-num {
    font-family: var(--sans);
    font-weight: 800;
    font-size: 28px;
    color: var(--text-bright);
    line-height: 1;
    margin-bottom: 4px;
  }

  .stat-label { font-size: 11px; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.1em; }

  /* SECTION COMMONS */
  section { padding: 100px 0; position: relative; z-index: 2; overflow: hidden; }

  .section-header { margin-bottom: 56px; }

  .section-tag {
    font-family: var(--code);
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .section-tag::before { content: '//'; color: var(--text-dim); }

  h2.section-title {
    font-family: var(--sans);
    font-weight: 800;
    font-size: clamp(28px, 4vw, 42px);
    color: var(--text-bright);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  h2.section-title.pulse-title {
    animation: title-pulse 3.4s ease-in-out infinite;
  }

  @keyframes title-pulse {
    0%, 100% { color: var(--text-dim); text-shadow: none; }
    50% { color: var(--text-bright); text-shadow: 0 0 18px rgba(0,212,255,0.35); }
  }

  .section-sub {
    margin-top: 12px;
    font-size: 13px;
    color: var(--text-dim);
    max-width: 480px;
  }

  /* DIVIDER */
  .divider {
    border: none;
    border-top: 1px solid var(--border);
    position: relative;
    z-index: 2;
  }

  /* PROJECTS */
  #projects { background: var(--bg); }

  .project-card {
    border: 1px solid var(--border);
    background: var(--bg2);
    margin-bottom: 2px;
    cursor: pointer;
    transition: border-color 0.2s;
    position: relative;
    overflow: hidden;
  }

  .project-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 3px; height: 0;
    background: var(--accent);
    transition: height 0.3s;
  }

  .project-card:hover { border-color: var(--border2); }
  .project-card:hover::before { height: 100%; }
  .project-card.active { border-color: var(--accent); }
  .project-card.active::before { height: 100%; }

  .project-header {
    padding: 24px 28px;
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 20px;
  }

  .project-meta { flex: 1; }

  .project-id {
    font-family: var(--code);
    font-size: 10px;
    color: var(--accent);
    letter-spacing: 0.1em;
    margin-bottom: 6px;
  }

  .project-name {
    font-family: var(--sans);
    font-weight: 700;
    font-size: 18px;
    color: var(--text-bright);
    margin-bottom: 6px;
  }

  .project-tagline { font-size: 12px; color: var(--text-dim); }

  .project-badges {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    align-items: flex-start;
  }

  .badge {
    padding: 3px 9px;
    font-size: 10px;
    font-family: var(--code);
    letter-spacing: 0.06em;
    border: 1px solid var(--border2);
    color: var(--text-dim);
    text-transform: uppercase;
    background: var(--bg3);
  }

  .badge-accent { border-color: var(--accent3); color: var(--accent); background: rgba(0,212,255,0.05); }
  .badge-amber { border-color: var(--amber-dim); color: var(--amber); background: rgba(245,166,35,0.05); }

  .expand-icon {
    font-size: 18px;
    color: var(--text-dim);
    transition: transform 0.3s, color 0.2s;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .project-card.active .expand-icon { transform: rotate(45deg); color: var(--accent); }

  .project-body {
    display: none;
    padding: 0 28px 28px;
    border-top: 1px solid var(--border);
    margin-top: 0;
  }

  .project-card.active .project-body { display: block; }

  .project-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-top: 24px;
  }

  @media (max-width: 680px) { .project-grid { grid-template-columns: 1fr; } }

  .proj-section { margin-bottom: 0; }

  .proj-section-label {
    font-family: var(--code);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 10px;
    padding-bottom: 6px;
    border-bottom: 1px solid var(--border);
  }

  .proj-section p { font-size: 13px; color: var(--text); line-height: 1.75; }

  .proj-section ul {
    list-style: none;
    padding: 0;
  }

  .proj-section ul li {
    font-size: 13px;
    color: var(--text);
    padding: 3px 0;
    padding-left: 16px;
    position: relative;
  }

  .proj-section ul li::before {
    content: '›';
    position: absolute;
    left: 0;
    color: var(--accent);
  }

  .failure-note {
    margin-top: 16px;
    padding: 14px 16px;
    background: rgba(255,68,68,0.05);
    border: 1px solid rgba(255,68,68,0.2);
    border-left: 3px solid var(--red);
    font-size: 12px;
    color: var(--text);
  }

  .failure-note .fn-label {
    font-family: var(--code);
    font-size: 10px;
    color: var(--red);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 6px;
  }

  .stack-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 4px;
  }

  .chip {
    padding: 4px 10px;
    font-family: var(--code);
    font-size: 11px;
    background: var(--bg3);
    border: 1px solid var(--border2);
    color: var(--text-dim);
  }

  /* SYSTEMS THINKING */
  #thinking { background: var(--bg2); }

  .insights-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2px;
  }

  .insight-card {
    background: var(--bg);
    border: 1px solid var(--border);
    padding: 28px;
    position: relative;
    transition: border-color 0.2s, background 0.2s;
  }

  .insight-card:hover {
    border-color: var(--border2);
    background: var(--bg3);
  }

  .insight-num {
    font-family: var(--code);
    font-size: 11px;
    color: var(--text-dim);
    margin-bottom: 16px;
  }

  .insight-topic {
    font-family: var(--sans);
    font-weight: 700;
    font-size: 15px;
    color: var(--text-bright);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .insight-body { font-size: 13px; color: var(--text); line-height: 1.8; }

  .insight-terminal {
    border: 1px solid var(--border2);
    background: var(--bg2);
    margin-bottom: 4px;
  }

  .insight-terminal .insight-terminal-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 10px;
    border-bottom: 1px solid var(--border);
    background: var(--bg3);
  }

  .insight-terminal .insight-terminal-bar .terminal-bar-label {
    margin-left: 6px;
    font-size: 9px;
  }

  .insight-terminal .insight-body {
    padding: 14px 16px;
    font-family: var(--code);
    font-size: 12px;
    min-height: 100px;
  }

  .insight-terminal .insight-body .term-cursor {
    display: inline-block;
    width: 6px; height: 11px;
    background: var(--accent);
    vertical-align: -1px;
    margin-left: 1px;
    animation: blink 1s infinite;
  }

  .insight-tag {
    display: inline-block;
    margin-top: 16px;
    font-family: var(--code);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--amber);
    border: 1px solid var(--amber-dim);
    padding: 2px 8px;
    background: rgba(245,166,35,0.04);
  }

  /* STACK */
  #stack { background: var(--bg); }

  .stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 2px;
  }

  .stack-group {
    background: var(--bg2);
    border: 1px solid var(--border);
    padding: 24px;
  }

  .stack-group-title {
    font-family: var(--code);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 16px;
    padding-bottom: 10px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .stack-items { display: flex; flex-wrap: wrap; gap: 6px; }

  .stack-item {
    font-family: var(--code);
    font-size: 12px;
    color: var(--text);
    padding: 5px 10px;
    border: 1px solid var(--border2);
    background: var(--bg3);
    transition: border-color 0.2s, color 0.2s, background 0.2s;
    filter: brightness(0.25) saturate(0.4);
  }

  .stack-item.flicker {
    animation-name: chip-flicker;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
    animation-direction: alternate;
  }

  @keyframes chip-flicker {
    0% { filter: brightness(0.25) saturate(0.4); }
    20% { filter: brightness(0.55) saturate(0.6); }
    40% { filter: brightness(0.35) saturate(0.5); }
    60% { filter: brightness(0.9) saturate(0.95); }
    80% { filter: brightness(0.45) saturate(0.55); }
    100% { filter: brightness(1) saturate(1); }
  }

  .stack-item:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: rgba(0,212,255,0.04);
    filter: brightness(1) saturate(1) !important;
    animation-play-state: paused;
  }

  .stack-item.core {
    border-color: rgba(0,212,255,0.3);
    color: var(--accent);
    background: rgba(0,212,255,0.06);
  }

  /* CERTS */
  #certs { background: var(--bg2); }

  .certs-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 2px;
  }

  .cert-item {
    background: var(--bg);
    border: 1px solid var(--border);
    padding: 20px 24px;
    display: flex;
    align-items: center;
    gap: 16px;
    transition: border-color 0.2s, transform 0.7s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.7s ease;
    opacity: 0;
  }

  .cert-item.fly-in {
    opacity: 1;
    transform: translate(0, 0) rotate(0deg) !important;
  }

  .cert-item:hover { border-color: var(--border2); }

  .cert-icon {
    width: 36px; height: 36px;
    border: 1px solid var(--border2);
    background: var(--bg3);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 16px;
    flex-shrink: 0;
    color: var(--accent);
    font-family: var(--code);
    font-size: 10px;
    font-weight: 500;
  }

  .cert-name { font-size: 13px; color: var(--text-bright); font-weight: 600; margin-bottom: 2px; }
  .cert-issuer { font-size: 11px; color: var(--text-dim); font-family: var(--code); }

  /* EXPERIENCE */
  #experience { background: var(--bg); }

  .timeline { position: relative; padding-left: 32px; }

  .timeline::before {
    content: '';
    position: absolute;
    left: 0; top: 8px; bottom: 0;
    width: 1px;
    background: linear-gradient(to bottom, var(--accent), var(--border), transparent);
  }

  .timeline-item {
    position: relative;
    margin-bottom: 48px;
  }

  .timeline-item::before {
    content: '';
    position: absolute;
    left: -36px; top: 8px;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 10px rgba(0,212,255,0.5);
  }

  .timeline-period {
    font-family: var(--code);
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.08em;
    margin-bottom: 6px;
  }

  .timeline-role {
    font-family: var(--sans);
    font-weight: 700;
    font-size: 17px;
    color: var(--text-bright);
    margin-bottom: 2px;
  }

  .timeline-company {
    font-size: 13px;
    color: var(--text-dim);
    margin-bottom: 14px;
  }

  .timeline-points { list-style: none; }

  .timeline-points li {
    font-size: 13px;
    color: var(--text);
    padding: 4px 0;
    padding-left: 18px;
    position: relative;
    line-height: 1.6;
  }

  .timeline-points li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--accent);
    font-size: 11px;
  }

  /* ABOUT */
  #about { background: var(--bg2); }

  .about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: start; }

  @media (max-width: 700px) { .about-grid { grid-template-columns: 1fr; } }

  .about-text p {
    font-size: 14px;
    color: var(--text);
    line-height: 1.9;
    margin-bottom: 20px;
  }

  .about-text strong { color: var(--text-bright); }

  .principles { }

  .principle-item {
    padding: 18px 0;
    border-bottom: 1px solid var(--border);
  }

  .principle-item:first-child { border-top: 1px solid var(--border); }

  .principle-title {
    font-family: var(--sans);
    font-weight: 700;
    font-size: 14px;
    color: var(--text-bright);
    margin-bottom: 4px;
  }

  .principle-desc { font-size: 12px; color: var(--text-dim); line-height: 1.6; }

  /* CONTACT */
  #contact { background: var(--bg); }

  .contact-wrap {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: start;
  }

  @media (max-width: 700px) { .contact-wrap { grid-template-columns: 1fr; } }

  .contact-statement {
    font-family: var(--sans);
    font-weight: 700;
    font-size: clamp(22px, 3vw, 32px);
    color: var(--text-bright);
    line-height: 1.3;
    margin-bottom: 20px;
  }

  .contact-statement span { color: var(--accent); }

  .contact-note { font-size: 13px; color: var(--text-dim); line-height: 1.7; }

  .contact-links { display: flex; flex-direction: column; gap: 12px; margin-top: 8px; }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px 20px;
    border: 1px solid var(--border);
    background: var(--bg2);
    text-decoration: none;
    color: var(--text);
    transition: background 0.2s, color 0.2s;
    box-shadow: 0 0 0 0 transparent;
  }

  .contact-link.beacon {
    animation-play-state: running;
  }

  @keyframes beacon-pulse {
    0%, 100% { border-color: var(--border); box-shadow: 0 0 0 0 transparent; }
    50% { border-color: var(--accent); box-shadow: 0 0 16px -2px rgba(0,212,255,0.6); }
  }

  .contact-link:hover {
    border-color: var(--accent);
    color: var(--text-bright);
    background: var(--bg3);
  }

  .contact-link-icon {
    width: 32px; height: 32px;
    border: 1px solid var(--border2);
    background: var(--bg3);
    display: flex; align-items: center; justify-content: center;
    font-size: 14px;
    flex-shrink: 0;
  }

  .contact-link-label { font-family: var(--code); font-size: 11px; color: var(--text-dim); }
  .contact-link-value { font-size: 13px; color: var(--text-bright); }

  /* FOOTER */
  footer {
    border-top: 1px solid var(--border);
    padding: 24px 0 50px;
    position: relative;
    z-index: 2;
  }

  .footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 32px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 11px;
    color: var(--text-dim);
    font-family: var(--code);
  }

  /* SCROLL REVEAL */
  .reveal {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* TERMINAL CURSOR */
  .cursor {
    display: inline-block;
    width: 8px; height: 14px;
    background: var(--accent);
    animation: blink 1s infinite;
    vertical-align: middle;
    margin-left: 2px;
  }

  @keyframes blink {
    0%, 49% { opacity: 1; }
    50%, 100% { opacity: 0; }
  }

  /* ARCHITECTURE DIAGRAM */
  .arch-diagram {
    background: var(--bg);
    border: 1px solid var(--border);
    padding: 24px;
    margin-top: 24px;
    overflow-x: auto;
  }

  .arch-diagram pre {
    font-family: var(--code);
    font-size: 12px;
    color: var(--text-dim);
    line-height: 1.6;
    white-space: pre;
  }

  .arch-diagram pre .hl { color: var(--accent); }
  .arch-diagram pre .hl2 { color: var(--amber); }
  .arch-diagram pre .hl3 { color: var(--green); }

  /* RESPONSIVE */
  @media (max-width: 600px) {
    .nav-links { display: none; }
    .hero-stats { gap: 24px; flex-wrap: wrap; }
    .container { padding: 0 20px; }
    .nav-inner { padding: 0 20px; }
  }
</style>
</head>
<body>

<div class="noise"></div>

<!-- NAV -->
<nav>
  <div class="nav-inner">
    <a href="#hero" class="nav-logo">AD<span>.engineer</span></a>
    <ul class="nav-links">
      <li><a href="#projects">Projects</a></li>
      <li><a href="#thinking">Thinking</a></li>
      <li><a href="#stack">Stack</a></li>
      <li><a href="#experience">Experience</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </div>
</nav>

<!-- STATUS BAR -->
<div class="status-bar">
  <div class="status-item">
    <div class="status-dot"></div>
    <span class="status-accent">AVAILABLE</span> for senior roles
  </div>
  <div class="status-item">Lagos, Nigeria → Remote / Relocation</div>
  <div class="status-item" style="margin-left:auto">CKA · CKAD · AWS · Terraform</div>
</div>

<!-- HERO -->
<section id="hero">
  <div class="hero-glow" data-parallax="0.15"></div>
  <div class="hero-glow-2" data-parallax="0.3"></div>
  <div class="hero-glow-3" data-parallax="0.45"></div>
  <div class="container">
    <div class="hero-content">

      <div class="hero-top">
        <div>
          <div class="hero-tag">DevOps · MLOps · Platform Engineering</div>
          <h1 class="hero-name">
            Amanyi<br>
            <span class="line2">Daniel</span>
          </h1>
        </div>
        <div class="hero-portrait-wrap lit" id="portraitWrap">
          <img class="hero-portrait" id="heroPortrait" src="images/portrait.jpg" alt="Amanyi Daniel">
          <span class="frame-corner tl"></span>
          <span class="frame-corner tr"></span>
          <span class="frame-corner bl"></span>
          <span class="frame-corner br"></span>
          <div class="portrait-tag">ID: AD-001 · VERIFIED</div>
        </div>
      </div>

      <div class="hero-terminal" id="heroTerminal">
        <div class="terminal-bar">
          <div class="terminal-dot"></div>
          <div class="terminal-dot"></div>
          <div class="terminal-dot"></div>
          <span class="terminal-bar-label">session — whoami</span>
        </div>
        <div class="terminal-body">
          <div class="term-prompt-line"><span class="term-accent">$</span> whoami — Senior Platform Engineer</div>
          <div class="term-output" id="termOutput"><span class="term-cursor"></span></div>
        </div>
      </div>

      <div class="hero-actions">
        <a href="#projects" class="btn btn-primary">View Projects</a>
        <a href="#contact" class="btn btn-ghost">Get in Touch</a>
      </div>
      <div class="hero-stats">
        <div>
          <div class="stat-num">4+</div>
          <div class="stat-label">Years Production</div>
        </div>
        <div>
          <div class="stat-num">7</div>
          <div class="stat-label">Certifications</div>
        </div>
        <div>
          <div class="stat-num">5</div>
          <div class="stat-label">Projects Shipped</div>
        </div>
        <div>
          <div class="stat-num">∞</div>
          <div class="stat-label">Incidents Survived</div>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- PROJECTS -->
<section id="projects">
  <div class="section-glow" data-parallax="0.2" style="top:-100px; left:-150px; background:radial-gradient(circle, rgba(0,212,255,0.04) 0%, transparent 70%);"></div>
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Engineering Work</div>
      <h2 class="section-title">Project Case Studies</h2>
      <p class="section-sub">Each project documented as an engineering breakdown — problem context, design decisions, trade-offs, and what I'd change.</p>
    </div>

    <!-- PROJECT 1: TITAN -->
    <div class="project-card reveal" onclick="toggleProject(this)">
      <div class="project-header">
        <div class="project-meta">
          <div class="project-id">PROJ-001 · INFRASTRUCTURE RESEARCH</div>
          <div class="project-name">TITAN — Bare-Metal Kubernetes Testbed</div>
          <div class="project-tagline">Fault tolerance semantics and controlled failure injection on a 3-node KVM/libvirt cluster</div>
        </div>
        <div class="project-badges">
          <span class="badge badge-accent">In Progress</span>
          <span class="badge">Kubernetes</span>
          <span class="badge">Bare-Metal</span>
        </div>
        <div class="expand-icon">+</div>
      </div>
      <div class="project-body">
        <div class="project-grid">
          <div class="proj-section">
            <div class="proj-section-label">Problem Context</div>
            <p>Managed Kubernetes services (EKS, GKE) abstract too much. When production incidents happen, I need to understand what the control plane is actually doing — not just what the cloud vendor reports. TITAN exists to study cluster behavior under real failure conditions: node loss, network partition, etcd degradation, and resource exhaustion.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Architecture Thinking</div>
            <p>Three KVM guests on a single physical host. One control plane node, two workers. Libvirt manages VM lifecycle; networking is intentionally constrained to simulate real inter-node latency. The point is not to replicate production — it's to create a controlled environment where I can inject failures and observe cascading behavior without paging anyone at 2am.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Stack</div>
            <div class="stack-chips">
              <span class="chip">KVM / libvirt</span>
              <span class="chip">kubeadm</span>
              <span class="chip">Cilium CNI</span>
              <span class="chip">Prometheus</span>
              <span class="chip">Grafana</span>
              <span class="chip">Chaos Mesh</span>
              <span class="chip">etcd</span>
            </div>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Constraints & Trade-offs</div>
            <ul>
              <li>Single-host colocation — failure domains overlap (intentional for this phase)</li>
              <li>No cloud networking — must simulate latency and partition manually</li>
              <li>Runbook-first discipline: every failure injection has a pre-defined runbook before execution</li>
              <li>Phase (-1) is purely a fluency gate — no research claims until baseline competency is verified</li>
            </ul>
          </div>
        </div>
        <div class="failure-note">
          <div class="fn-label">⚠ What I'd Do Differently</div>
          Three nodes on one host gives you operational control but loses true failure domain isolation. The next evolution requires physical separation — either dedicated mini-PCs or rented bare-metal. I'm documenting this limitation explicitly so that any research output from TITAN carries the correct caveats.
        </div>
      </div>
    </div>

    <!-- PROJECT 2: TELEMETRY STREAMLINE -->
    <div class="project-card reveal" onclick="toggleProject(this)">
      <div class="project-header">
        <div class="project-meta">
          <div class="project-id">PROJ-002 · DATA PIPELINE</div>
          <div class="project-name">Telemetry Streamline — Serverless Event Pipeline</div>
          <div class="project-tagline">Real-time telemetry ingestion on AWS: Kinesis → Lambda → DynamoDB / S3 / OpenSearch</div>
        </div>
        <div class="project-badges">
          <span class="badge badge-accent">Shipped</span>
          <span class="badge">AWS</span>
          <span class="badge">Serverless</span>
        </div>
        <div class="expand-icon">+</div>
      </div>
      <div class="project-body">
        <div class="project-grid">
          <div class="proj-section">
            <div class="proj-section-label">Problem Context</div>
            <p>The brief was to ingest high-velocity telemetry events, fan them out across three storage backends (hot, cold, and search), and do it without managing servers. The real constraint: the cost model had to stay predictable — no provisioned capacity bills appearing at end of month.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Architecture Decisions</div>
            <p>Kinesis Data Streams as the ingest buffer gives replay capability and decouples producers from consumers. Lambda shards off the stream — one function per concern. DynamoDB for hot read access (single-digit ms latency SLA), S3 for cold archival, OpenSearch for full-text query. The fan-out happens inside Lambda, not via SNS — this reduces hop count and keeps the critical path tight.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Stack</div>
            <div class="stack-chips">
              <span class="chip">AWS Kinesis</span>
              <span class="chip">Lambda (Python)</span>
              <span class="chip">DynamoDB</span>
              <span class="chip">S3</span>
              <span class="chip">OpenSearch</span>
              <span class="chip">IAM</span>
              <span class="chip">CloudWatch</span>
            </div>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Challenges</div>
            <ul>
              <li>Lambda cold start latency on the OpenSearch write path needed a keep-warm strategy</li>
              <li>DynamoDB write amplification from hot partition keys — resolved with composite key design</li>
              <li>IAM scope creep: initial role was too permissive; hardened to resource-level least privilege</li>
              <li>OpenSearch cluster sizing: started too small, index rotation policy required mid-flight</li>
            </ul>
          </div>
        </div>
        <div class="failure-note">
          <div class="fn-label">⚠ Failure Log</div>
          First deployment had no dead-letter queue. An upstream schema change caused Lambda to throw silently — events dropped with no alert. Lesson: every event pipeline needs a DLQ and an alarm on it before anything else ships. This is now a checklist item I apply to every async architecture.
        </div>
      </div>
    </div>

    <!-- PROJECT 3: PKI/mTLS -->
    <div class="project-card reveal" onclick="toggleProject(this)">
      <div class="project-header">
        <div class="project-meta">
          <div class="project-id">PROJ-003 · SECURITY ENGINEERING</div>
          <div class="project-name">PKI / mTLS Service Mesh Engagement</div>
          <div class="project-tagline">Zero-trust service identity for a client microservices environment using mutual TLS and internal CA</div>
        </div>
        <div class="project-badges">
          <span class="badge badge-accent">Consulting</span>
          <span class="badge">Security</span>
          <span class="badge">Kubernetes</span>
        </div>
        <div class="expand-icon">+</div>
      </div>
      <div class="project-body">
        <div class="project-grid">
          <div class="proj-section">
            <div class="proj-section-label">Problem Context</div>
            <p>Client had 14 microservices communicating over plain HTTP inside a Kubernetes cluster. Compliance requirements mandated in-transit encryption with mutual authentication — not just TLS, but provable service identity. The team had no PKI experience and needed an operational solution, not a proof of concept.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Design Approach</div>
            <p>Internal CA provisioned via cert-manager on the cluster. Certificates issued per-service using workload identity (SPIFFE). mTLS enforced at the sidecar proxy layer — no application code changes required. Certificate rotation automated with a 24-hour TTL; rotation failures trigger PagerDuty before expiry. Root CA stored offline; intermediate CA rotated annually.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Stack</div>
            <div class="stack-chips">
              <span class="chip">cert-manager</span>
              <span class="chip">Istio</span>
              <span class="chip">SPIFFE/SPIRE</span>
              <span class="chip">Vault PKI</span>
              <span class="chip">Kubernetes</span>
              <span class="chip">OpenSSL</span>
            </div>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Trade-offs Made</div>
            <ul>
              <li>Sidecar proxy adds ~15ms p99 latency per hop — acceptable given compliance requirement</li>
              <li>Vault PKI backend chosen over self-managed CA for audit trail and secrets governance</li>
              <li>Short-lived certs (24h) increase rotation ops burden — mitigated by full automation</li>
              <li>SPIFFE over manual CN — harder to implement, but eliminates human error in cert naming</li>
            </ul>
          </div>
        </div>
        <div class="failure-note">
          <div class="fn-label">⚠ What Almost Went Wrong</div>
          During rollout, a misconfigured NetworkPolicy blocked the cert-manager webhook from reaching the API server. Services that failed cert renewal silently degraded instead of erroring loudly. Root cause: no integration test for cert issuance path in staging. Added a synthetic cert-request probe to the monitoring stack immediately after.
        </div>
      </div>
    </div>

    <!-- PROJECT 4: SRIM -->
    <div class="project-card reveal" onclick="toggleProject(this)">
      <div class="project-header">
        <div class="project-meta">
          <div class="project-id">PROJ-004 · PRACTITIONER WRITING</div>
          <div class="project-name">SRIM — Systems Resilience Infrastructure Manual</div>
          <div class="project-tagline">21-chapter practitioner manuscript on infrastructure resilience engineering</div>
        </div>
        <div class="project-badges">
          <span class="badge badge-amber">Writing</span>
          <span class="badge">Resilience</span>
          <span class="badge">Architecture</span>
        </div>
        <div class="expand-icon">+</div>
      </div>
      <div class="project-body">
        <div class="project-grid">
          <div class="proj-section">
            <div class="proj-section-label">What It Is</div>
            <p>A 21-chapter practitioner-focused manual covering infrastructure resilience from first principles. Five main parts plus a philosophy chapter and bridge chapter — written from operational experience, not theory. Covers failure modes, recovery design, observability, runbook discipline, and the human factors in incident response.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Why I Wrote It</div>
            <p>Most resilience content is either too abstract (academic papers) or too narrow (vendor documentation). SRIM is the resource I wished existed when I started dealing with production incidents — opinionated, practitioner-grade, and grounded in systems that actually failed. Writing it forced me to articulate assumptions I'd been operating on implicitly for years.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- PROJECT 5: SMART INVOICE AI PIPELINE -->
    <div class="project-card reveal" onclick="toggleProject(this)">
      <div class="project-header">
        <div class="project-meta">
          <div class="project-id">PROJ-005 · AI/ML INFRASTRUCTURE</div>
          <div class="project-name">Smart Invoice AI Pipeline</div>
          <div class="project-tagline">Cloud-native ML document processing: S3 → Textract → Bedrock → DynamoDB, fully Terraform-managed</div>
        </div>
        <div class="project-badges">
          <span class="badge badge-accent">Shipped</span>
          <span class="badge">AWS</span>
          <span class="badge badge-amber">AI/ML</span>
        </div>
        <div class="expand-icon">+</div>
      </div>
      <div class="project-body">
        <div class="project-grid">
          <div class="proj-section">
            <div class="proj-section-label">Problem Context</div>
            <p>Invoice processing is a high-volume, error-prone manual operation in most finance workflows. The goal was to build a production-ready serverless pipeline that handles raw document uploads, extracts structured fields using real AI services, and persists results — all with pay-per-use cost characteristics and zero server management.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Architecture Design</div>
            <p>Two Lambda functions — one for upload handling (API Gateway → S3), one for inference — decoupled via S3 event trigger. Textract handles OCR and form extraction; Bedrock (Claude 3 Haiku) runs intelligent field normalization for vendor names, amounts, and dates that rule-based extraction misses. Results land in DynamoDB for downstream consumption. All infra is modular Terraform — six discrete modules covering compute, storage, IAM, API Gateway, and both Lambda functions.</p>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Stack</div>
            <div class="stack-chips">
              <span class="chip">AWS Lambda (Python)</span>
              <span class="chip">AWS Textract</span>
              <span class="chip">Amazon Bedrock</span>
              <span class="chip">Claude 3 Haiku</span>
              <span class="chip">S3</span>
              <span class="chip">DynamoDB</span>
              <span class="chip">API Gateway</span>
              <span class="chip">Terraform</span>
              <span class="chip">IAM</span>
            </div>
          </div>
          <div class="proj-section">
            <div class="proj-section-label">Trade-offs & Design Decisions</div>
            <ul>
              <li>Hybrid extraction strategy — rule-based for common patterns, AI for edge cases — keeps Bedrock costs to ~$0.0005/invoice</li>
              <li>S3 event trigger over SNS: fewer hops, tighter coupling acceptable here since upload and inference are always paired</li>
              <li>DynamoDB as output store: optimized for downstream single-key reads, not analytics — a warehouse would be added for reporting use</li>
              <li>Modular Terraform over a monolith: each AWS resource category is independently versioned and reusable across projects</li>
              <li>Total per-invoice cost: ~$0.002 — Textract dominates at $0.0015/page</li>
            </ul>
          </div>
        </div>
        <div class="failure-note">
          <div class="fn-label">⚠ What I'd Do Differently</div>
          No dead-letter queue on the inference Lambda in v1 — a Bedrock throttle or Textract failure would silently drop the event. Also, confidence scoring from Textract isn't surfaced in the final output schema yet; adding a <code style="font-family:var(--code);font-size:11px;color:var(--accent)">confidence_breakdown</code> field per extracted field would make result quality auditable downstream. Both are documented as v2 items.
        </div>
        <div style="margin-top:14px">
          <a href="https://github.com/DanielAmanyi/smart-invoice-infra" target="_blank" class="btn btn-ghost" style="font-size:11px;padding:8px 16px;display:inline-flex">
            ↗ View on GitHub
          </a>
        </div>
      </div>
    </div>

  </div>
</section>
<section id="thinking">
  <div class="section-glow" data-parallax="0.25" style="top:20%; right:-200px; background:radial-gradient(circle, rgba(245,166,35,0.035) 0%, transparent 70%);"></div>
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Engineering Philosophy</div>
      <h2 class="section-title">How I Think About Systems</h2>
      <p class="section-sub">Opinionated takes from real operational experience. Not definitions — how I actually approach problems.</p>
    </div>

    <div class="insights-grid" id="insightsGrid">
      <div class="insight-card reveal">
        <div class="insight-num">01</div>
        <div class="insight-topic">Observability is a design decision, not a retrofit</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — observability</span>
          </div>
          <div class="insight-body" data-text="If you're adding metrics after an incident, you've already lost. Observability needs to be specified at design time — what questions will operators ask at 3am? Those questions determine your instrumentation. Logs that don't answer operational questions are just storage costs."></div>
        </div>
        <div class="insight-tag">Observability</div>
      </div>

      <div class="insight-card reveal">
        <div class="insight-num">02</div>
        <div class="insight-topic">Runbooks that aren't tested aren't runbooks</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — operations</span>
          </div>
          <div class="insight-body" data-text="A runbook written during a postmortem and never executed again is documentation theater. Every runbook in my environments has an associated drill schedule. If the steps are wrong when it's calm, they're catastrophically wrong when it's not. This is the TITAN doctrine applied operationally."></div>
        </div>
        <div class="insight-tag">Operations</div>
      </div>

      <div class="insight-card reveal">
        <div class="insight-num">03</div>
        <div class="insight-topic">Toil is a capacity problem disguised as a process problem</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — platform-eng</span>
          </div>
          <div class="insight-body" data-text="Teams that do a lot of manual operational work usually frame it as &quot;we just need better runbooks.&quot; The real problem is that every hour spent on toil is an hour not spent on reliability improvements. Automation isn't about laziness — it's about preserving engineering capacity for work that actually requires thinking."></div>
        </div>
        <div class="insight-tag">Platform Engineering</div>
      </div>

      <div class="insight-card reveal">
        <div class="insight-num">04</div>
        <div class="insight-topic">Blast radius matters more than failure probability</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — reliability</span>
          </div>
          <div class="insight-body" data-text="I care less about how likely a failure is than about how much damage it causes. A 0.01% probability event that takes down your entire data plane is more dangerous than a 10% probability event affecting one service. Architecture decisions should be driven by consequence, not likelihood."></div>
        </div>
        <div class="insight-tag">Reliability</div>
      </div>

      <div class="insight-card reveal">
        <div class="insight-num">05</div>
        <div class="insight-topic">CI/CD pipelines are the first indicator of team health</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — ci-cd</span>
          </div>
          <div class="insight-body" data-text="Show me a team's deployment pipeline and I can tell you how they work. Flaky tests that &quot;everyone knows to ignore,&quot; multi-hour build times, no staging parity — these aren't CI/CD problems. They're signals about code ownership, test discipline, and whether engineering leadership is measuring the right things."></div>
        </div>
        <div class="insight-tag">CI/CD</div>
      </div>

      <div class="insight-card reveal">
        <div class="insight-num">06</div>
        <div class="insight-topic">Kubernetes is not a deployment target — it's an operating model</div>
        <div class="insight-terminal">
          <div class="insight-terminal-bar">
            <span class="terminal-dot"></span><span class="terminal-dot"></span><span class="terminal-dot"></span>
            <span class="terminal-bar-label">note — infrastructure</span>
          </div>
          <div class="insight-body" data-text="Organizations that treat Kubernetes as &quot;just a better way to deploy containers&quot; consistently struggle with it. The operational model — declarative state, controller reconciliation, pod lifecycle — requires a different mental model than traditional server-based thinking. You're not managing machines anymore; you're managing desired state."></div>
        </div>
        <div class="insight-tag">Infrastructure</div>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- STACK -->
<section id="stack">
  <div class="section-glow" data-parallax="0.2" style="bottom:-150px; left:30%; background:radial-gradient(circle, rgba(0,212,255,0.035) 0%, transparent 70%);"></div>
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Technology</div>
      <h2 class="section-title">Technical Stack</h2>
      <p class="section-sub">Grouped by function. Everything listed has been used in production systems.</p>
    </div>

    <div class="stack-grid">
      <div class="stack-group reveal">
        <div class="stack-group-title">☁ Cloud Platforms</div>
        <div class="stack-items">
          <span class="stack-item core">AWS</span>
          <span class="stack-item">GCP</span>
          <span class="stack-item">Azure</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">⎈ Container Orchestration</div>
        <div class="stack-items">
          <span class="stack-item core">Kubernetes (EKS)</span>
          <span class="stack-item core">Rancher</span>
          <span class="stack-item">Bare-Metal K8s</span>
          <span class="stack-item">Helm</span>
          <span class="stack-item">ArgoCD</span>
          <span class="stack-item">Docker</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">⚙ Infrastructure as Code</div>
        <div class="stack-items">
          <span class="stack-item core">Terraform</span>
          <span class="stack-item">Ansible</span>
          <span class="stack-item">CloudFormation</span>
          <span class="stack-item">Pulumi</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">📡 Observability</div>
        <div class="stack-items">
          <span class="stack-item core">Prometheus</span>
          <span class="stack-item core">Grafana</span>
          <span class="stack-item">OpenTelemetry</span>
          <span class="stack-item">Loki</span>
          <span class="stack-item">CloudWatch</span>
          <span class="stack-item">PagerDuty</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">🔄 CI/CD</div>
        <div class="stack-items">
          <span class="stack-item core">Jenkins</span>
          <span class="stack-item">GitHub Actions</span>
          <span class="stack-item">GitLab CI</span>
          <span class="stack-item">ArgoCD</span>
          <span class="stack-item">Tekton</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">🔐 Security & Networking</div>
        <div class="stack-items">
          <span class="stack-item core">cert-manager</span>
          <span class="stack-item core">Vault</span>
          <span class="stack-item">Istio</span>
          <span class="stack-item">Cilium</span>
          <span class="stack-item">SPIFFE</span>
          <span class="stack-item">OPA</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">🤖 AI / ML Platform (Emerging)</div>
        <div class="stack-items">
          <span class="stack-item">vLLM</span>
          <span class="stack-item">Triton</span>
          <span class="stack-item">Kubeflow</span>
          <span class="stack-item">MLflow</span>
          <span class="stack-item">Ray</span>
        </div>
      </div>

      <div class="stack-group reveal">
        <div class="stack-group-title">💻 Languages & Scripting</div>
        <div class="stack-items">
          <span class="stack-item core">Python</span>
          <span class="stack-item core">Bash</span>
          <span class="stack-item">Go</span>
          <span class="stack-item">YAML</span>
          <span class="stack-item">HCL</span>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- CERTIFICATIONS -->
<section id="certs">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Credentials</div>
      <h2 class="section-title">Certifications</h2>
    </div>
    <div class="certs-list">
      <div class="cert-item cert-fly">
        <div class="cert-icon">CKA</div>
        <div>
          <div class="cert-name">Certified Kubernetes Administrator</div>
          <div class="cert-issuer">CNCF / Linux Foundation</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">CKAD</div>
        <div>
          <div class="cert-name">Certified Kubernetes App Developer</div>
          <div class="cert-issuer">CNCF / Linux Foundation</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">SAA</div>
        <div>
          <div class="cert-name">AWS Solutions Architect Associate</div>
          <div class="cert-issuer">Amazon Web Services</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">DVA</div>
        <div>
          <div class="cert-name">AWS Developer Associate</div>
          <div class="cert-issuer">Amazon Web Services</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">SOA</div>
        <div>
          <div class="cert-name">AWS SysOps Administrator Associate</div>
          <div class="cert-issuer">Amazon Web Services</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">TF</div>
        <div>
          <div class="cert-name">HashiCorp Terraform Associate</div>
          <div class="cert-issuer">HashiCorp</div>
        </div>
      </div>
      <div class="cert-item cert-fly">
        <div class="cert-icon">N+</div>
        <div>
          <div class="cert-name">CompTIA Network+</div>
          <div class="cert-issuer">CompTIA</div>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- EXPERIENCE -->
<section id="experience">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Work History</div>
      <h2 class="section-title" id="experienceTitle">Experience</h2>
    </div>

    <div class="timeline">
      <div class="timeline-item reveal">
        <div class="timeline-period">Jan 2026 – Present</div>
        <div class="timeline-role">DevOps / Platform Engineer — Consultant</div>
        <div class="timeline-company">Networksx (Australia)</div>
        <ul class="timeline-points">
          <li>Designed mTLS-based identity architecture for distributed AI agent communication — evaluated CRL vs OCSP trade-offs under failure and latency conditions</li>
          <li>Built certificate lifecycle and key rotation strategies for large-scale deployments; identified trust-chain and control-plane failure risks</li>
          <li>Advised on resilience patterns and blast radius containment strategies across distributed systems</li>
        </ul>
      </div>

      <div class="timeline-item reveal">
        <div class="timeline-period">May 2024 – May 2025</div>
        <div class="timeline-role">DevOps Engineer</div>
        <div class="timeline-company">Capshall</div>
        <ul class="timeline-points">
          <li>Designed and operated AWS infrastructure with Terraform across multiple environments — resolved state drift, enforced IaC consistency</li>
          <li>Built CI/CD pipelines (Jenkins, CodePipeline) improving deployment reliability and eliminating manual intervention</li>
          <li>Managed Kubernetes workloads on EKS: scheduling issues, resource contention, networking misconfigurations</li>
          <li>Deployed observability stack (Prometheus, Grafana, ELK) for real-time monitoring and incident debugging</li>
          <li>Hardened IAM boundaries and improved reliability across production workloads</li>
        </ul>
      </div>

      <div class="timeline-item reveal">
        <div class="timeline-period">Oct 2022 – Apr 2024</div>
        <div class="timeline-role">Cloud Engineer</div>
        <div class="timeline-company">Cloudboosta</div>
        <ul class="timeline-points">
          <li>Provisioned multi-AZ AWS infrastructure with Terraform and CloudFormation, reducing manual provisioning overhead</li>
          <li>Supported serverless and microservices systems across Lambda, API Gateway, and ECS</li>
          <li>Designed secure VPC architectures, IAM policies, and network isolation strategies</li>
          <li>Optimized cloud cost and performance through right-sizing and resource planning</li>
        </ul>
      </div>

      <div class="timeline-item reveal">
        <div class="timeline-period">Jun 2020 – Oct 2022</div>
        <div class="timeline-role">Systems Engineer</div>
        <div class="timeline-company">Urban Cloudworks</div>
        <ul class="timeline-points">
          <li>Managed Linux-based infrastructure and web hosting environments (Nginx, Apache)</li>
          <li>Automated operational workflows using Bash and Python</li>
          <li>Maintained system uptime and resolved production issues across client environments</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- ABOUT -->
<section id="about">
  <div class="container">
    <div class="section-header reveal">
      <div class="section-tag">Identity</div>
      <h2 class="section-title">About</h2>
    </div>

    <div class="about-grid">
      <div class="about-text reveal">
        <p>
          I'm a DevOps and Platform Engineer based in Lagos, Nigeria, with 4+ years of production experience across AWS, Kubernetes, and distributed infrastructure. My work sits at the intersection of <strong>systems reliability, platform automation, and operational engineering</strong>.
        </p>
        <p>
          I'm not a tool operator — I'm an engineer who understands why systems fail and how to build them so that when they do, the damage is contained and the recovery is fast. I've designed mTLS identity architectures, built serverless AI pipelines, operated EKS and bare-metal Kubernetes, and written the runbooks that others follow.
        </p>
        <p>
          Currently building toward <strong>AI Platform Engineering</strong> — specifically the infrastructure layer for inference systems: GPU-aware scheduling, serving stacks (vLLM, Triton), and AI-specific observability. Also actively building TITAN, a bare-metal Kubernetes research testbed focused on distributed system behavior and fault tolerance semantics.
        </p>
        <p>
          BSc Computer Science, Crawford University Nigeria (CGPA 4.3/5.0). Open to senior roles with relocation to Europe or Canada.
        </p>
      </div>

      <div class="principles reveal">
        <div class="principle-item">
          <div class="principle-title">Ownership over process</div>
          <div class="principle-desc">I take end-to-end responsibility for systems I build. If something I deployed caused an incident, I'm the first call — not the last.</div>
        </div>
        <div class="principle-item">
          <div class="principle-title">Depth over breadth</div>
          <div class="principle-desc">I'd rather understand five tools deeply than have surface familiarity with twenty. Real operational judgment comes from seeing the failure modes, not the happy path.</div>
        </div>
        <div class="principle-item">
          <div class="principle-title">Production is the only ground truth</div>
          <div class="principle-desc">Staging environments lie. The real behavior of a distributed system only emerges under real load, real failures, and real operators making real decisions under pressure.</div>
        </div>
        <div class="principle-item">
          <div class="principle-title">Documentation is engineering output</div>
          <div class="principle-desc">Runbooks, architecture decisions, postmortems — these are as much a part of the system as the code. A system that only one person understands is a liability.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="divider">

<!-- CONTACT -->
<section id="contact">
  <div class="container">
    <div class="contact-wrap">
      <div class="reveal">
        <div class="section-tag" style="margin-bottom:16px">Contact</div>
        <div class="contact-statement">
          If you're building serious infrastructure,<br>
          let's <span>talk</span>.
        </div>
        <p class="contact-note">
          Available for senior DevOps, Platform Engineering, and MLOps roles. Open to remote positions and relocation to Europe or Canada. Response time: usually within 24 hours.
        </p>
      </div>

      <div class="contact-links reveal">
        <a href="mailto:amanyidaniel.io@gmail.com" class="contact-link beacon-target">
          <div class="contact-link-icon">✉</div>
          <div>
            <div class="contact-link-label">Email</div>
            <div class="contact-link-value">amanyidaniel.io@gmail.com</div>
          </div>
        </a>
        <a href="https://github.com/DanielAmanyi" target="_blank" class="contact-link beacon-target">
          <div class="contact-link-icon">⌥</div>
          <div>
            <div class="contact-link-label">GitHub</div>
            <div class="contact-link-value">github.com/DanielAmanyi</div>
          </div>
        </a>
        <a href="https://www.linkedin.com/in/daniel-amanyi-87169472/" target="_blank" class="contact-link beacon-target">
          <div class="contact-link-icon">in</div>
          <div>
            <div class="contact-link-label">LinkedIn</div>
            <div class="contact-link-value">linkedin.com/in/daniel-amanyi-87169472</div>
          </div>
        </a>
        <a href="https://drive.google.com/file/d/117H8aZBLxY6oqdsjl64aFsa2QfM53GJi/view?usp=sharing" target="_blank" class="contact-link beacon-target">
          <div class="contact-link-icon">↓</div>
          <div>
            <div class="contact-link-label">Resume</div>
            <div class="contact-link-value">Download CV (PDF)</div>
          </div>
        </a>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-inner">
    <span>Amanyi Daniel — Platform Engineer · amanyidaniel.io@gmail.com</span>
    <span>Lagos, Nigeria · <span style="color:var(--accent)">Open to Relocation</span></span>
  </div>
</footer>

<script>
  // Parallax engine — elements drift at a fraction of scroll speed via data-parallax
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const parallaxEls = prefersReducedMotion ? [] : Array.from(document.querySelectorAll('[data-parallax]')).map(el => ({
    el,
    speed: parseFloat(el.getAttribute('data-parallax'))
  }));

  let ticking = false;
  function updateParallax() {
    const scrollY = window.scrollY;
    parallaxEls.forEach(({ el, speed }) => {
      el.style.transform = `translateY(${scrollY * speed * -1}px)`;
    });
    ticking = false;
  }

  window.addEventListener('scroll', () => {
    if (!ticking) {
      requestAnimationFrame(updateParallax);
      ticking = true;
    }
  }, { passive: true });

  updateParallax();

  // Hero terminal: typing is linked directly to scroll position (scrub
  // forward to type, scrub back to untype) — independent of the portrait,
  // which stays static at full brightness with no automation at all.
  const termOutputEl = document.getElementById('termOutput');
  const heroPortrait = document.getElementById('heroPortrait');
  const heroSectionEl = document.getElementById('hero');

  const TERMINAL_TEXT = "DevOps and Platform Engineer with 4+ years designing and operating Kubernetes-native systems across AWS and on-prem environments. Strong focus on reliability engineering, failure handling, and infrastructure consistency. Known for diagnosing complex infra failures, minimizing blast radius, and maintaining control plane stability under stress.";

  if (heroPortrait) {
    heroPortrait.style.filter = 'brightness(1) saturate(1) contrast(1.05)';
  }

  function typeText(el, text, baseSpeed) {
    let i = 0;
    el.innerHTML = '<span class="tc"></span><span class="term-cursor"></span>';
    const tcSpan = el.querySelector('.tc');
    function step() {
      i++;
      tcSpan.textContent = text.slice(0, i);
      if (i < text.length) {
        // Natural human typing rhythm: base speed with randomized jitter,
        // plus a slightly longer pause after punctuation/spaces.
        const ch = text[i - 1];
        let delay = baseSpeed + (Math.random() * baseSpeed * 0.6 - baseSpeed * 0.3);
        if (ch === ' ') delay += 30;
        if (ch === ',' || ch === '—' || ch === '.') delay += 120;
        setTimeout(step, delay);
      } else {
        const cursor = el.querySelector('.term-cursor');
        if (cursor) cursor.remove();
      }
    }
    step();
  }

  // Scroll-scrubbed typing for the hero terminal specifically: character
  // count is derived directly from how far the user has scrolled through
  // the hero's own height, so it types forward and untypes backward in
  // lockstep with scroll position. No locking, no scroll interception —
  // the page scrolls completely normally underneath this. The completion
  // distance is a fraction of the viewport height (not a fixed px value)
  // so it reliably finishes while the hero is still mostly in view,
  // regardless of screen size.
  if (termOutputEl && heroSectionEl) {
    let lastHeroCharCount = -1;

    function getHeroTypeScrollDistance() {
      return window.innerHeight * 0.28;
    }

    function updateHeroTyping() {
      const heroTop = heroSectionEl.offsetTop;
      const scrollY = window.scrollY;
      let progress = (scrollY - heroTop) / getHeroTypeScrollDistance();
      progress = Math.max(0, Math.min(1, progress));

      const charCount = Math.floor(progress * TERMINAL_TEXT.length);
      if (charCount !== lastHeroCharCount) {
        const shown = TERMINAL_TEXT.slice(0, charCount);
        const cursor = progress < 1 ? '<span class="term-cursor"></span>' : '';
        termOutputEl.innerHTML = `<span class="tc">${shown}</span>${cursor}`;
        lastHeroCharCount = charCount;
      }
    }

    window.addEventListener('scroll', () => {
      requestAnimationFrame(updateHeroTyping);
    }, { passive: true });

    window.addEventListener('resize', updateHeroTyping);
    updateHeroTyping();
  }


  // Scroll reveal
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry, i) => {
      if (entry.isIntersecting) {
        setTimeout(() => entry.target.classList.add('visible'), 80);
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });
  reveals.forEach(el => observer.observe(el));

  // Certifications fly in from random directions when scrolled into view
  const certItems = document.querySelectorAll('.cert-fly');
  certItems.forEach((el) => {
    const angle = Math.random() * Math.PI * 2;
    const distance = 140 + Math.random() * 100;
    const x = Math.cos(angle) * distance;
    const y = Math.sin(angle) * distance;
    const rotate = (Math.random() * 24 - 12).toFixed(1);
    el.style.transform = `translate(${x.toFixed(0)}px, ${y.toFixed(0)}px) rotate(${rotate}deg)`;
  });
  const certObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('fly-in');
        certObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.2 });
  certItems.forEach(el => certObserver.observe(el));

  // "Experience" heading pulses slowly dark-to-bright while its section is in view
  const experienceTitle = document.getElementById('experienceTitle');
  if (experienceTitle) {
    const experienceObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        experienceTitle.classList.toggle('pulse-title', entry.isIntersecting);
      });
    }, { threshold: 0.15 });
    experienceObserver.observe(experienceTitle);
  }

  // Contact card "beacon" chase — one card at a time, in random order,
  // each pulsing low-to-bright (theme cyan only) 4-5 times before handing
  // off to a different randomly chosen card. Runs only while the Contact
  // section is in view.
  const beaconCards = Array.from(document.querySelectorAll('.beacon-target'));
  const contactSection = document.getElementById('contact');
  let beaconActive = false;
  let beaconTimeoutId = null;
  let lastBeaconCard = null;

  function runBeaconOnce() {
    if (!beaconActive || beaconCards.length === 0) return;

    let card;
    if (beaconCards.length === 1) {
      card = beaconCards[0];
    } else {
      do {
        card = beaconCards[Math.floor(Math.random() * beaconCards.length)];
      } while (card === lastBeaconCard);
    }
    lastBeaconCard = card;

    const pulseCount = 4 + Math.floor(Math.random() * 2); // 4 or 5 pulses
    let completedPulses = 0;

    card.style.animationName = 'beacon-pulse';
    card.style.animationDuration = '0.55s';
    card.style.animationTimingFunction = 'ease-in-out';
    card.style.animationIterationCount = String(pulseCount);
    card.classList.add('beacon');

    function onIteration() {
      completedPulses++;
    }

    function onEnd() {
      card.classList.remove('beacon');
      card.style.animationName = '';
      card.removeEventListener('animationiteration', onIteration);
      card.removeEventListener('animationend', onEnd);
      if (beaconActive) {
        beaconTimeoutId = setTimeout(runBeaconOnce, 300 + Math.random() * 500);
      }
    }

    card.addEventListener('animationiteration', onIteration);
    card.addEventListener('animationend', onEnd);
  }

  function startBeaconChase() {
    if (beaconActive) return;
    beaconActive = true;
    runBeaconOnce();
  }

  function stopBeaconChase() {
    beaconActive = false;
    if (beaconTimeoutId) {
      clearTimeout(beaconTimeoutId);
      beaconTimeoutId = null;
    }
  }

  if (contactSection && beaconCards.length > 0) {
    const beaconObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) startBeaconChase();
        else stopBeaconChase();
      });
    }, { threshold: 0.2 });
    beaconObserver.observe(contactSection);
  }

  // Stack item flicker — each tech chip independently flickers brightness forever,
  // like unstable neon signs, once scrolled into view. Each gets randomized
  // duration + a negative delay so they're never in sync with each other.
  const stackGroups = document.querySelectorAll('.stack-group');
  const stackObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const items = entry.target.querySelectorAll('.stack-item');
        items.forEach((item) => {
          const duration = 2.2 + Math.random() * 3.5; // 2.2s–5.7s per chip
          const negDelay = -(Math.random() * duration); // start mid-cycle at a random point
          item.style.animationDuration = `${duration.toFixed(2)}s`;
          item.style.animationDelay = `${negDelay.toFixed(2)}s`;
          item.classList.add('flicker');
        });
        stackObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.25 });
  stackGroups.forEach(group => stackObserver.observe(group));

  // Project toggle
  function toggleProject(card) {
    const wasActive = card.classList.contains('active');
    document.querySelectorAll('.project-card').forEach(c => c.classList.remove('active'));
    if (!wasActive) card.classList.add('active');
  }

  // Engineering Philosophy insight cards: each body types out character by
  // character inside its terminal box, once scrolled into view, at the
  // same human-paced typing speed as the hero terminal. Each card's start
  // is randomly delayed so they don't all begin typing in lockstep.
  const insightBodies = document.querySelectorAll('.insight-terminal .insight-body');
  function typeInsightBody(el) {
    const text = el.getAttribute('data-text') || '';
    typeText(el, text, 45);
  }

  const insightObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const randomStartDelay = Math.random() * 900; // 0–900ms randomized stagger
        setTimeout(() => typeInsightBody(entry.target), randomStartDelay);
        insightObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.4 });
  insightBodies.forEach(el => insightObserver.observe(el));

  // Slowed scrolling through "Engineering Philosophy" only. Activates only
  // once the section's own top edge has scrolled into the viewport (not
  // merely "any pixel of it is touching the bottom edge of the screen",
  // which was previously causing it to trigger too early — while still
  // inside the tail end of the Projects section above it).
  //
  // The damped zone starts at the section's top edge (the "Engineering
  // Philosophy" heading) and ends precisely at the bottom of the insight
  // cards grid (the "Kubernetes is not a deployment target..." card) —
  // not the section's outer padding, so it doesn't drift past that card.
  const dampedSectionStart = document.getElementById('thinking');
  const dampedZoneEnd = document.getElementById('insightsGrid');

  const DAMPING_FACTOR = 0.385; // 35% base, +10% speed = 0.385

  function isWithinDampedZone(scrollY) {
    if (!dampedSectionStart || !dampedZoneEnd) return false;
    const top = dampedSectionStart.offsetTop;
    const bottom = dampedZoneEnd.offsetTop + dampedZoneEnd.offsetHeight;
    return scrollY >= top && scrollY < bottom;
  }

  function handleDampedWheel(e) {
    const scrollY = window.scrollY;
    if (!isWithinDampedZone(scrollY)) return; // let native scroll happen

    e.preventDefault();
    window.scrollBy(0, e.deltaY * DAMPING_FACTOR);
  }

  window.addEventListener('wheel', handleDampedWheel, { passive: false });

  let dampedTouchStartY = null;
  function handleDampedTouchStart(e) {
    dampedTouchStartY = e.touches[0].clientY;
  }
  function handleDampedTouchMove(e) {
    if (dampedTouchStartY === null) return;
    const currentY = e.touches[0].clientY;
    const deltaY = dampedTouchStartY - currentY;
    const scrollY = window.scrollY;
    if (isWithinDampedZone(scrollY)) {
      e.preventDefault();
      window.scrollBy(0, deltaY * DAMPING_FACTOR);
      dampedTouchStartY = currentY;
    } else {
      dampedTouchStartY = currentY;
    }
  }
  window.addEventListener('touchstart', handleDampedTouchStart, { passive: true });
  window.addEventListener('touchmove', handleDampedTouchMove, { passive: false });

  // Active nav on scroll
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('.nav-links a');

  window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(s => {
      if (window.scrollY >= s.offsetTop - 80) current = s.id;
    });
    navLinks.forEach(link => {
      link.style.color = link.getAttribute('href') === '#' + current
        ? 'var(--accent)' : '';
    });
  });
</script>
</body>
</html>
